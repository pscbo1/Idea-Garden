# Upload to GitHub

Create a new empty GitHub repository. Upload the contents of this folder, not
the folder itself and not a ZIP file. After uploading, the GitHub repository
home page must show `package.json`, `app`, and `public` immediately.

Then import that repository as a new Vercel project with Root Directory set to
`./` and Framework Preset set to Next.js.
